# Infra

- `docker-compose.yml` yerel geliştirme içindir.
- `k8s/` klasöründe API, web ve gateway için örnek Deployment + Service + Ingress tanımları bulunur.
- `terraform/` klasörü PostgreSQL, Redis, MQTT ve nesne depolaması için altyapı iskeletini içerir.
